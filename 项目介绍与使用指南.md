# iPad互联：项目介绍与使用指南

## 项目作用

iPad互联是一款 Windows 自用副屏工具。它在 Windows 端创建并捕获虚拟显示器，将画面以 H.264 发送到前台运行 OpenDisplay 的 iPad，并把触控、滚动和 Apple Pencil 输入送回 Windows。

当前发布版本为 **v0.2.0**，基于 OpenDisplay Windows 发送端进行加固，支持 USB/Wi-Fi、Bonjour/mDNS 动态发现、稳定设备 ID、设备优先级和有序故障转移。

## 使用前准备

- Windows 10/11 x64。
- iPad 上安装并打开 OpenDisplay；串流期间需要保持其在前台。
- Windows 安装 Parsec Virtual Display Driver。该第三方驱动不随本项目安装包分发。
- Wi-Fi 使用仅适合可信的家庭或私人局域网；OpenDisplay v3 本身未提供加密和身份认证。

## 安装与连接

1. 按仓库 `README.md` 的驱动安全说明安装并核验 Parsec VDD。
2. 在 GitHub **Releases** 下载 `iPad互联-Setup-x64.exe` 并安装。
3. 在 iPad 上打开 OpenDisplay。
4. 启动 iPad互联，从发现到的设备中选择目标并点击“连接”；也可在高级设置中填写私网地址。
5. 需要快速切换时选择另一台设备，程序会先结束旧会话，再启动新的单目标会话。
6. 关闭窗口后程序可留在托盘；请从托盘菜单断开或退出。

安装包未签名，运行前请核对 Release 页面的 SHA-256。

## 已知边界

- USB 只改变传输通道，不能替代 iPad 端前台运行 OpenDisplay。
- 不支持控制原生 iPadOS 应用、音频、剪贴板或 H.265。
- 多台不同原生分辨率的 iPad 会受 Parsec VDD 单一自定义分辨率限制。
- 稳定设备 ID 用于连续识别设备，不是安全认证。

## 从源码构建

需要 Visual Studio/Build Tools 的 C++ 桌面工作负载、Windows SDK 和 CMake 3.25 或更高版本。

```powershell
.\scripts\build.ps1
```

生成安装包：

```powershell
.\scripts\package.ps1 -Version 0.2.0
```

## 发布状态

本仓库保存 v0.2.0 最终源码，安装包通过 GitHub Release 分发。本地构建、测试和模拟接收端检查不能代替真实 iPad、多设备、DHCP 变化、长时间串流及干净 Windows 安装验收。
